# Kokoro local model

TalentHunt OS expects these runtime assets in this directory:

- `kokoro-v1.0.int8.onnx`
- `voices-v1.0.bin`

They are distributed with the `model-files-v1.0` release of
`thewh1teagle/kokoro-onnx` and are intentionally excluded from Git.
